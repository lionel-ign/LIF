
<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function(){
  const toggles = document.querySelectorAll('button.project-toggle');
  toggles.forEach(btn => {
    btn.addEventListener('click', function(){
      const section = btn.dataset.section;
      const hiddenProjects = document.querySelectorAll('#' + section + ' .hidden-project');
      const isHidden = hiddenProjects[0].style.display === 'none' || hiddenProjects[0].style.display === '';
      hiddenProjects.forEach(el => el.style.display = isHidden ? 'block' : 'none');
      btn.textContent = isHidden ? 'Show less' : 'Show more';
    });
  });
});
</script>
